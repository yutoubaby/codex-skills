# Codex Skills

我的个人 Codex Skills 合集。

## Included skills

- [`prompt-optimizer`](skills/prompt-optimizer/)：将模糊需求改写为清晰、可执行、可验证且带安全边界的提示词。

## Structure

每个 Skill 位于 `skills/<skill-name>/`，并包含必需的 `SKILL.md` 与可选的 `agents/openai.yaml`、`references/`、`scripts/` 或 `assets/`。
